#цель: зависимость
#[tab] команда


BIN = ServerChatSM
SOURCES = main.c
HEADERS = main.h
CC := gcc

all:
	$(CC) $(SOURCES) -lrt -o $(BIN).out
$(BIN): $(HEADERS)

run: all
	./$(BIN).out
#clean:
#	rm -rf $(BIN) *.o $(BIN).out



















